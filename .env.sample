PORT="4545"

# Path to a custom settings.json file (optional).
# If not set, defaults to settings.json in the project root.
# SETTINGS_FILE="/path/to/settings.json"

# ---------------------------------------------------------------------------
# SETTINGS MANAGED VIA settings.json (NOT .env)
# ---------------------------------------------------------------------------
# The following values are stored in settings.json and can be updated at
# runtime via GET/PUT /api/settings.  They are seeded from these env vars on
# the FIRST run only; after that the .env values for these keys are ignored.
#
# Operational settings:
#   LOG_LEVEL="warn"
#   LOGTAIL_ENDPOINT="https://in.logs.betterstack.com"
#   BROWSER_RENDERER="puppeteer"    # puppeteer | cloudflare | browserless | multi
#   BROWSERLESS_IO_ENDPOINT=""
#   ENABLE_CLOUDFLARE_BROWSER_RENDERING="false"
#   ENABLE_BROWSERLESS_IO="false"
#   CHROMIUM_BIN=""
#   MAX_IMAGES_TO_KEEP=1000
#
# API keys / tokens (can also be stored in settings.json instead of here):
#   WEATHER_APIKEY=""
#   TODOIST_APIKEY=""
#   GOOGLE_CLIENT_ID=""
#   GOOGLE_CLIENT_SECRET=""
#   GOOGLE_REFRESH_TOKEN=""
#   CLOUDFLARE_ACCOUNT_ID=""
#   CLOUDFLARE_API_TOKEN=""
#   BROWSERLESS_IO_TOKEN=""
#   LOGTAIL_SOURCE_TOKEN=""
#
# Values in settings.json take precedence over the env vars above.
# ---------------------------------------------------------------------------

# Better Stack (formerly Logtail) - Remote logging (Optional)
# Get your source token from: https://logs.betterstack.com/
LOGTAIL_SOURCE_TOKEN=""

WEATHER_APIKEY=""

# Google Calendar API credentials
# See src/callbacks/calendar/index.ts for setup instructions
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_REFRESH_TOKEN=""

SENTRY_DSN=""

# Required when BROWSER_RENDERER=cloudflare (set via /api/settings)
CLOUDFLARE_ACCOUNT_ID=""
CLOUDFLARE_API_TOKEN=""

# Required when BROWSER_RENDERER=browserless (set via /api/settings)
BROWSERLESS_IO_TOKEN=""

# Admin interface password protection
# REQUIRED – the admin UI and /api/settings endpoints will be inaccessible
# without this value being set.
ADMIN_PASSWORD=""
TODOIST_APIKEY=""
