PORT="4545"

# Log level (env-only, not stored in settings.json)
# Options: trace | debug | info | warn | error | fatal | silent
LOG_LEVEL="warn"

# Path to a custom settings.json file (optional).
# If not set, defaults to settings.json in the project root.
# SETTINGS_FILE="/path/to/settings.json"

# ---------------------------------------------------------------------------
# SETTINGS MANAGED VIA settings.json
# ---------------------------------------------------------------------------
# The following values are stored and updated in settings.json at runtime
# via GET/PUT /api/settings.  Set them directly in the file or via the API.
# Do NOT set them here – this .env file is for bootstrap/secret values only.
#
# Operational:
#   browserRenderer       puppeteer | cloudflare | browserless | multi
#   browserlessEndpoint   ""
#   enableCloudflareBrowserRendering  false
#   enableBrowserlessIO   false
#   chromiumBin           ""
#   maxImagesToKeep       1000
#   logtailEndpoint       "https://in.logs.betterstack.com"
#
# API keys / tokens (set in settings.json, NOT here):
#   weatherApiKey
#   todoistApiKey
#   googleClientId
#   googleClientSecret
#   googleRefreshToken
#   cloudflareAccountId
#   cloudflareApiToken
#   browserlessIoToken
#   logtailSourceToken
# ---------------------------------------------------------------------------

# Admin interface password protection
# REQUIRED – the admin UI and /api/settings endpoints will be inaccessible
# without this value being set.
ADMIN_PASSWORD=""

SENTRY_DSN=""
